apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "WORDS_COUNTER_BATCH.fullname" . }}-test-connection"
  labels:
    {{- include "WORDS_COUNTER_BATCH.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "WORDS_COUNTER_BATCH.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
