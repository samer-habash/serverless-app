apiVersion: v1
kind: Service
metadata:
  name: {{ include "WORDS_COUNTER_BATCH.fullname" . }}
  labels:
    {{- include "WORDS_COUNTER_BATCH.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "WORDS_COUNTER_BATCH.selectorLabels" . | nindent 4 }}
